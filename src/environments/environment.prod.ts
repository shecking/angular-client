// This file represents the production environment, containing any variables that will run the app while in production.

// When the app is built for production, the command `ng build --prod` will replace environment.ts with environment.prod.ts

export const environment = {
  production: true,
  apiUrl: 'http://localhost:4741'
};
